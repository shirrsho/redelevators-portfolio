// Twitter card image — same asset as the Open Graph image. Re-exported so we
// keep a single source and Next emits twitter:image alongside og:image.
export { default, alt, size, contentType } from "./opengraph-image";
